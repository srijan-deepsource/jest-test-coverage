{
  "scripts": {
    "test": "jest --coverage=true --coverageReporters=cobertura"
  },
  "devDependencies": {
    "jest": "^26.2.2",
    "prettier": "^2.0.5"
  },
  "jest": {
    "collectCoverage": true,
    "coverageReporters": ["cobertura"]
  }
}
